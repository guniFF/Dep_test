# docker-compose-resource.yml
services:
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "8080:8080"
    networks:
      - mynetwork

networks:
  mynetwork:
    external: true
